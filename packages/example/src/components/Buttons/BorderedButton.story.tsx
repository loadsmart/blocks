import { BorderedButton } from '@loadsmart/building-blocks'
import { storiesOf } from '@storybook/react-native'
import React from 'react'

storiesOf('Buttons', module).add('Bordered Button', () => {
  return <BorderedButton />
})
